## config.md
```
# Config Update
# USE FOR: Update NeuroVision project configuration in .claude/config.json
# WHEN: Need to modify project settings, add systems, update paths, or change features
# EXAMPLE: "Add new brain structure to key_systems" or "Update performance targets" or "Enable experimental features"
claude execute --mcp-server filesystem-config --prompt "Update NeuroVision .claude/config.json. Handle JSON properly and backup existing config. Current project: professional medical education platform with Material 3 UI, autoload architecture, and Intel UHD 620 optimization: {{INPUT}}"
```
